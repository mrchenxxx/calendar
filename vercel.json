{
    "builds": [{
        "src": "main.py",
        "use": "vercel-python",
        "config": {
            "handler": "main:app",
            "runtime": "python3.9"
        }
    }],
    "routes": [
        {
            "src": "/(.*)",
            "dest": "main.py"
        }
    ]
}